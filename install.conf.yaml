- clean: [ '~', '~/.config' ]

- shell:
    - mkdir -p ~/.config

- defaults:
    link:
        create: true

- link:
    ~/.bashrc: bashrc
    ~/.gitconfig: gitconfig
    ~/.i3blocks.conf: i3blocks.conf
    ~/.vimrc: vimrc
    ~/.config/i3/config: config/i3/config
    ~/.config/vifm/vifmrc: config/vifm/vifmrc

